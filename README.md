# تطبيق الطلب الرقمي - Digital Order App

تطبيق أندرويد لعرض موقع **digitalorder.store** باستخدام WebView مع جميع الميزات الحديثة.

## 📱 معلومات التطبيق

- **الاسم**: الطلب الرقمي (Digital Order)
- **الإصدار**: 1.0.0
- **Package Name**: com.digitalorder.store
- **الموقع**: https://digitalorder.store

## ✨ الميزات

### الميزات الأساسية
- عرض الموقع بشكل كامل داخل التطبيق
- دعم كامل لـ JavaScript والمحتوى التفاعلي
- شاشة بداية جذابة مع شعار التطبيق
- شريط تقدم لعرض حالة التحميل
- السحب للأسفل لتحديث الصفحة
- أيقونة مخصصة للتطبيق

### معالجة الأخطاء
- التحقق من الاتصال بالإنترنت
- عرض رسائل خطأ واضحة
- زر إعادة المحاولة عند الفشل

### التنقل
- التنقل داخل الموقع باستخدام زر الرجوع
- الضغط مرتين على زر الرجوع للخروج من التطبيق
- فتح الروابط الخارجية في المتصفح

### الروابط الخاصة
- دعم روابط الهاتف (tel:)
- دعم روابط البريد الإلكتروني (mailto:)
- دعم روابط واتساب (whatsapp:)
- دعم روابط الرسائل (sms:)

## 🛠️ المتطلبات التقنية

### للتطوير
- **Android Studio**: Arctic Fox أو أحدث
- **JDK**: 8 أو أحدث
- **Gradle**: 8.1.0
- **Min SDK**: 21 (Android 5.0)
- **Target SDK**: 34 (Android 14)

### للتشغيل
- جهاز أندرويد بإصدار 5.0 أو أحدث
- اتصال بالإنترنت

## 📂 هيكل المشروع

```
DigitalOrderApp/
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── java/com/digitalorder/store/
│   │       │   ├── MainActivity.java          # الشاشة الرئيسية
│   │       │   └── SplashActivity.java        # شاشة البداية
│   │       ├── res/
│   │       │   ├── drawable/                  # الصور والأيقونات
│   │       │   ├── layout/                    # ملفات التصميم
│   │       │   ├── mipmap-*/                  # أيقونات التطبيق
│   │       │   ├── values/                    # النصوص والألوان
│   │       │   └── xml/                       # إعدادات XML
│   │       └── AndroidManifest.xml            # ملف البيان
│   ├── build.gradle                           # إعدادات البناء
│   └── proguard-rules.pro                     # قواعد ProGuard
├── gradle/                                    # ملفات Gradle
├── build.gradle                               # إعدادات المشروع
├── settings.gradle                            # إعدادات المشروع
├── gradle.properties                          # خصائص Gradle
├── VERSION.md                                 # سجل الإصدارات
└── README.md                                  # هذا الملف
```

## 🚀 البدء

### 1. فتح المشروع في Android Studio

```bash
# افتح Android Studio
# اختر File > Open
# حدد مجلد DigitalOrderApp
```

### 2. مزامنة المشروع

بعد فتح المشروع، سيقوم Android Studio تلقائياً بمزامنة Gradle. إذا لم يحدث ذلك:
- اضغط على **File > Sync Project with Gradle Files**

### 3. تشغيل التطبيق

#### على جهاز حقيقي:
1. قم بتفعيل **خيارات المطور** و **USB Debugging** على جهازك
2. وصل الجهاز بالكمبيوتر عبر USB
3. اضغط على زر **Run** (▶️) في Android Studio
4. اختر جهازك من القائمة

#### على محاكي:
1. افتح **AVD Manager** من Android Studio
2. أنشئ محاكي جديد أو استخدم موجود
3. اضغط على زر **Run** (▶️)
4. اختر المحاكي من القائمة

## 📦 بناء APK

### بناء APK للاختبار (Debug)

```bash
./gradlew assembleDebug
```

الملف الناتج: `app/build/outputs/apk/debug/app-debug.apk`

### بناء APK للنشر (Release)

```bash
./gradlew assembleRelease
```

الملف الناتج: `app/build/outputs/apk/release/app-release-unsigned.apk`

**ملاحظة**: للنشر على Google Play، ستحتاج إلى توقيع التطبيق بمفتاح خاص.

## 🔧 التخصيص

### تغيير عنوان URL الموقع

افتح ملف `app/src/main/res/values/strings.xml` وعدل:

```xml
<string name="website_url">https://your-website.com</string>
```

### تغيير اسم التطبيق

في نفس الملف:

```xml
<string name="app_name">اسم التطبيق الجديد</string>
```

### تغيير الألوان

افتح `app/src/main/res/values/colors.xml` وعدل الألوان:

```xml
<color name="primary">#1A5F7A</color>
<color name="accent">#57C5B6</color>
```

### تغيير الشعار

استبدل الملف `app/src/main/res/drawable/logo.png` بشعارك الجديد.

### تغيير أيقونة التطبيق

استبدل الملفات في المجلدات:
- `app/src/main/res/mipmap-mdpi/ic_launcher.png`
- `app/src/main/res/mipmap-hdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xhdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xxhdpi/ic_launcher.png`
- `app/src/main/res/mipmap-xxxhdpi/ic_launcher.png`

## 📝 تحديث الإصدار

عند إضافة ميزات جديدة أو إصلاح أخطاء:

1. افتح `app/build.gradle`
2. حدث `versionCode` و `versionName`:

```gradle
defaultConfig {
    versionCode 2
    versionName "1.0.1"
}
```

3. حدث `app/src/main/res/values/strings.xml`:

```xml
<string name="version">الإصدار 1.0.1</string>
```

4. وثق التغييرات في `VERSION.md`

## 🐛 حل المشاكل

### المشروع لا يبني بشكل صحيح

1. تأكد من تثبيت أحدث إصدار من Android Studio
2. نظف المشروع: **Build > Clean Project**
3. أعد البناء: **Build > Rebuild Project**
4. امسح الكاش: **File > Invalidate Caches / Restart**

### WebView لا يعرض الموقع

1. تأكد من إضافة صلاحية الإنترنت في `AndroidManifest.xml`
2. تأكد من أن الجهاز متصل بالإنترنت
3. تحقق من عنوان URL في `strings.xml`

### التطبيق يتوقف عند التشغيل

1. افحص سجلات الأخطاء في **Logcat**
2. تأكد من أن جميع الملفات موجودة
3. تأكد من صحة أسماء الحزم (package names)

## 📄 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه وتعديله بحرية.

## 📞 الدعم

للأسئلة والاستفسارات، يرجى التواصل عبر:
- الموقع: https://digitalorder.store

---

**تم التطوير بواسطة Manus AI**


---

## 🚀 بناء APK تلقائي باستخدام GitHub Actions

تم إعداد هذا المشروع لبناء ملفات APK تلقائيًا في كل مرة يتم فيها رفع تحديثات إلى المستودع على GitHub. اتبع الخطوات التالية للاستفادة من هذه الميزة.

### الخطوة 1: إنشاء مستودع جديد على GitHub

1.  اذهب إلى [GitHub](https://github.com) وقم بتسجيل الدخول.
2.  أنشئ مستودعًا جديدًا (New Repository). يمكنك تسميته `DigitalOrderApp`.

### الخطوة 2: رفع ملفات المشروع إلى المستودع

1.  تأكد من أن لديك جميع ملفات المشروع، بما في ذلك مجلد `.github`.
2.  ارفع جميع الملفات والمجلدات إلى المستودع الذي أنشأته على GitHub.

### الخطوة 3: متابعة عملية البناء

- بمجرد رفع الملفات، ستبدأ عملية البناء تلقائيًا.
- يمكنك متابعة التقدم من خلال الذهاب إلى تبويب **Actions** في مستودعك على GitHub.

### الخطوة 4: تحميل ملف APK

بعد انتهاء عملية البناء بنجاح، يمكنك تحميل ملف APK من قسم **Releases** على الصفحة الرئيسية للمستودع.
