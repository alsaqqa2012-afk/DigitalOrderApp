# Digital Order App - تطبيق الطلب الرقمي

**الإصدار**: 1.0.2  
**تاريخ الإصدار**: 11 نوفمبر 2025  
**المنصة**: Android (API 21+)

---

## نظرة عامة

تطبيق **الطلب الرقمي** هو تطبيق Android يعرض موقع [digitalorder.store](https://digitalorder.store) في واجهة WebView محسّنة وآمنة. يوفر التطبيق تجربة تصفح سلسة مع ميزات متقدمة للأمان والأداء.

---

## الميزات الرئيسية

### الأمان والحماية 🔒
يتميز الإصدار 1.0.2 بتحسينات أمنية كبيرة تضمن حماية بيانات المستخدمين. تم تعطيل الاتصالات غير المشفرة (HTTP) بالكامل، مما يفرض استخدام HTTPS فقط لجميع الاتصالات. كما تم تطبيق معالجة صارمة لأخطاء شهادات SSL، حيث يتم رفض أي شهادة غير موثوقة تلقائياً لحماية المستخدمين من هجمات Man-in-the-Middle.

### تجربة المستخدم 📱
يوفر التطبيق واجهة سهلة الاستخدام مع شاشة بداية احترافية تعرض شعار التطبيق. يدعم التطبيق السحب للأسفل لتحديث الصفحة، مع شريط تقدم يعرض حالة التحميل بشكل مرئي. تم تطبيق نظام الضغط المزدوج للخروج من التطبيق لتجنب الإغلاق غير المقصود.

### الوظائف المتقدمة ⚙️
يدعم التطبيق JavaScript بالكامل لتشغيل المحتوى التفاعلي، مع إمكانية التنقل بزر الرجوع عبر تاريخ التصفح. تم إضافة دعم كامل لتحميل الملفات من الموقع مباشرة إلى جهاز المستخدم. يتم حفظ حالة التصفح تلقائياً عند تدوير الشاشة أو تغيير الإعدادات.

### الروابط الخاصة 🔗
يتعامل التطبيق بذكاء مع الروابط الخاصة مثل روابط الهاتف (tel:)، البريد الإلكتروني (mailto:)، واتساب (whatsapp:)، والرسائل النصية (sms:). يتم فتح هذه الروابط في التطبيقات المناسبة تلقائياً، بينما تُفتح الروابط الخارجية في المتصفح الافتراضي.

---

## المتطلبات التقنية

| المتطلب | القيمة |
|---------|--------|
| **الحد الأدنى لإصدار Android** | Android 5.0 (API 21) |
| **الإصدار المستهدف** | Android 14 (API 34) |
| **أدوات البناء** | Gradle 8.0+ |
| **لغة البرمجة** | Java 8 |
| **المكتبات الرئيسية** | AndroidX, Material Components |

---

## البناء والتثبيت

### متطلبات التطوير
يتطلب بناء التطبيق توفر Android Studio Arctic Fox أو أحدث، مع Java Development Kit (JDK) 8 أو أحدث، وAndroid SDK مع API 34.

### خطوات البناء

1. **استنساخ المشروع**
   ```bash
   git clone <repository-url>
   cd DigitalOrderApp
   ```

2. **فتح المشروع في Android Studio**
   - افتح Android Studio
   - اختر "Open an Existing Project"
   - حدد مجلد المشروع

3. **مزامنة Gradle**
   ```bash
   ./gradlew sync
   ```

4. **البناء للتطوير**
   ```bash
   ./gradlew assembleDebug
   ```

5. **البناء للإنتاج**
   ```bash
   ./gradlew assembleRelease
   ```

---

## البنية التقنية

### الملفات الرئيسية

يتكون التطبيق من ثلاثة ملفات Java رئيسية. **SplashActivity.java** يدير شاشة البداية التي تظهر لمدة ثانيتين مع معالجة محسّنة لتسرب الذاكرة. **MainActivity.java** هو القلب النابض للتطبيق، حيث يدير WebView مع جميع الإعدادات الأمنية والوظيفية. **DownloadHelper.java** هو فئة مساعدة جديدة تدير عملية تحميل الملفات باستخدام DownloadManager.

### الموارد والتخطيطات

تم تصميم واجهة المستخدم باستخدام Material Design مع دعم كامل للغة العربية (RTL). يتضمن التطبيق تخطيطين رئيسيين: **activity_splash.xml** لشاشة البداية، و**activity_main.xml** للشاشة الرئيسية مع WebView وعناصر التحكم.

---

## الإصلاحات في الإصدار 1.0.2

### الإصلاحات الأمنية الحرجة

تم في هذا الإصدار معالجة مشكلتين أمنيتين حرجتين. الأولى هي إصلاح معالجة أخطاء SSL، حيث كان الإصدار السابق يسمح للمستخدم بتجاوز أخطاء الشهادات، مما يفتح الباب لهجمات خطيرة. الآن يتم رفض جميع أخطاء SSL تلقائياً مع عرض رسالة واضحة للمستخدم. المشكلة الثانية كانت السماح بالاتصالات غير المشفرة (HTTP)، والتي تم تعطيلها بالكامل لضمان تشفير جميع الاتصالات.

### التحديثات التقنية

تم استبدال دالة `onBackPressed()` المهملة بـ `OnBackPressedCallback` الحديثة لضمان التوافق مع إصدارات Android المستقبلية. كما تم تحسين إعدادات المحتوى المختلط من السماح الكامل إلى وضع التوافق لتحقيق توازن أفضل بين الأمان والوظائف.

### الميزات الجديدة

أضيفت ميزة تحميل الملفات بالكامل، مما يسمح للمستخدمين بتحميل الملفات من الموقع مباشرة إلى أجهزتهم. تم تطبيق هذه الميزة باستخدام فئة مساعدة جديدة تدير العملية بشكل آمن وفعال.

---

## الاستخدام

### التشغيل الأساسي

عند تشغيل التطبيق لأول مرة، ستظهر شاشة البداية لمدة ثانيتين، ثم ينتقل التطبيق تلقائياً إلى الشاشة الرئيسية. يتم التحقق من الاتصال بالإنترنت تلقائياً، وفي حالة عدم توفره، تظهر رسالة خطأ واضحة مع زر لإعادة المحاولة.

### التنقل والتصفح

يمكن استخدام زر الرجوع في الجهاز للتنقل عبر تاريخ التصفح داخل الموقع. عند الوصول إلى بداية التاريخ، يتطلب الخروج من التطبيق الضغط على زر الرجوع مرتين خلال ثانيتين. يمكن تحديث الصفحة في أي وقت بالسحب للأسفل من أعلى الشاشة.

### تحميل الملفات

عند النقر على رابط تحميل في الموقع، يبدأ التحميل تلقائياً باستخدام DownloadManager في Android. تظهر رسالة تأكيد مع اسم الملف، ويمكن متابعة التقدم من خلال إشعارات النظام. يتم حفظ الملفات في مجلد التنزيلات الافتراضي.

---

## الأمان والخصوصية

يطبق التطبيق أعلى معايير الأمان لحماية بيانات المستخدمين. جميع الاتصالات مشفرة باستخدام HTTPS فقط، مع رفض تلقائي لأي شهادات SSL غير موثوقة. لا يتم جمع أي بيانات شخصية من قبل التطبيق نفسه، حيث تخضع جميع البيانات لسياسة خصوصية الموقع digitalorder.store.

---

## الدعم والمساهمة

للإبلاغ عن مشاكل أو اقتراح تحسينات، يرجى فتح Issue في مستودع المشروع. نرحب بجميع المساهمات من خلال Pull Requests، مع الالتزام بمعايير الكود المتبعة في المشروع.

---

## الترخيص

هذا المشروع مرخص بموجب [اسم الترخيص]. يرجى الاطلاع على ملف LICENSE للمزيد من التفاصيل.

---

## الشكر والتقدير

تم تطوير وإصلاح هذا التطبيق بواسطة **Manus AI** بتاريخ 11 نوفمبر 2025.

---

**للمزيد من المعلومات**: راجع ملف CHANGELOG_v1.0.2.md لتفاصيل كاملة عن جميع التحديثات والإصلاحات.
